import ReactDOM from "react-dom/client";
import App from "./App";

// Create the root element and render the App component into #root div in public/index.html
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
